services:
  rafflehub.api:
    image: rafflehub.api
    build:
      context: .
      dockerfile: RaffleHub.Api/Dockerfile
    environment:
      - "CONNECTIONSTRINGS__DEFAULTCONNECTION=Host=SEU_HOST_AQUI; Database=SEU_BANCO; Username=SEU_USUARIO; Password=SUA_SENHA; SSL Mode=Require;"
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "5000:8080"

volumes:
  pg_data_v3:

